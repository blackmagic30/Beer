name: "Pint Path production JavaScript and TypeScript"

# Test fixtures intentionally exercise unsafe filesystem and credential shapes.
# Keep CodeQL focused on code that ships or runs operationally; the test suite
# remains covered by TypeScript, Vitest, the repository security scan, and CI.
paths-ignore:
  - "test/**"
